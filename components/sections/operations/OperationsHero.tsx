import { PageHero } from "@/components/sections/PageHero";

export function OperationsHero() {
  return (
    <PageHero
      eyebrow="Behind the gate"
      title="Everyone who runs the building, on one line"
      subhead="Committee, facility manager, supervisors, guards, housekeeping and vendors — with the work assigned, the handovers logged and the escalations owned. Not a group chat that scrolls away every night."
    />
  );
}
